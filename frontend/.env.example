# URL de base de l'API backend (sans slash final).
# En local : défaut = http://localhost:8000/api
# En prod (ex. Cloudflare Pages) : définir à l'URL réelle du backend (ex. https://votre-backend.up.railway.app/api)
# VITE_API_URL=http://localhost:8000/api
